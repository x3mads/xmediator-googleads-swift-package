// Intentionally empty.
// This wrapper target exists so the XMediatorGoogleAds product can combine the
// XMediatorGoogleAds binary target and carry their required
// system-framework link settings (binary targets cannot declare either).
